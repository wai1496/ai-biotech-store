const {denyProduction,intentIdFromRef}=require('./_shared');
module.exports=async function handler(req,res){try{denyProduction();if(req.method!=='GET')return res.status(405).send('GET required');const q=req.query||{},statusId=String(q.status_id||''),intent=String(q.intent||intentIdFromRef(q.order_id)||'');const status=statusId==='1'?'success':statusId==='2'?'pending':'failed';const target=new URL('/member.html',`https://${req.headers['x-forwarded-host']||req.headers.host}`);target.searchParams.set('wallet_topup',status);if(intent)target.searchParams.set('intent',intent);if(q.billcode)target.searchParams.set('billcode',String(q.billcode));return res.redirect(302,target.toString())}catch(err){console.error(err);return res.status(err.statusCode||500).send(err.message||'Wallet return failed')}};