(()=>{
'use strict';
const cfg=window.AIBT_CONFIG||{},db=window.supabase?.createClient(cfg.supabaseUrl,cfg.supabaseKey);if(!db)return;
const $=s=>document.querySelector(s),esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot',"'":'&#39;'}[c])),money=v=>'RM '+Number(v||0).toFixed(2),dt=v=>v?new Date(v).toLocaleString():'—';
let epReady=false;
async function token(){const {data:{session}}=await db.auth.getSession();if(!session?.access_token)throw new Error('Staging admin session expired.');return session.access_token}
async function api(path,body){const t=await token(),r=await fetch(`/api/staging-shipping/${path}`,{method:'POST',headers:{'content-type':'application/json',authorization:`Bearer ${t}`},body:JSON.stringify(body||{})}),j=await r.json();if(!r.ok)throw new Error(j?.error||`EasyParcel ${path} failed`);return j}
function show(title,subtitle,body,foot='<button class="btn" onclick="closeOpsDialog()">Close</button>'){const d=$('#opsDialog');$('#dialogTitle').textContent=title;$('#dialogSubtitle').textContent=subtitle||'';$('#dialogBody').innerHTML=body;$('#dialogFoot').innerHTML=foot;d.showModal()}
async function render(){document.querySelector('#opsTitle').textContent='Shipping & Tracking';document.querySelector('#opsSubtitle').textContent='EasyParcel connection, rate, booking, waybill and status sync';document.querySelectorAll('#opsNav [data-view]').forEach(b=>b.classList.toggle('active',b.dataset.view==='shipping'));document.querySelector('#opsSidebar')?.classList.remove('show');const [ir,or,sr]=await Promise.all([db.from('integration_configs').select('enabled,mode,status,config').eq('integration_key','easyparcel').maybeSingle(),db.from('orders').select('id,order_number,user_id,status,payment_status,grand_total,created_at').order('created_at',{ascending:false}).limit(100),db.from('shipments').select('*').order('updated_at',{ascending:false})]);const integ=ir.data||{},orders=or.data||[],ships=sr.data||[];const oauth=!!integ.config?.oauth_connected;epReady=!!integ.enabled&&(integ.status==='healthy'||integ.status==='connected')&&oauth;const map=new Map(ships.map(s=>[s.order_id,s]));$('#opsContent').innerHTML=`<section class="card"><div class="card-head"><div><h2>EasyParcel Adapter</h2><p class="muted">Connect from here — no Postman or copied authorization code required.</p></div><span class="badge ${epReady?'green':'amber'}">${epReady?'CONNECTED':'SETUP REQUIRED'}</span></div><div class="health-list"><div class="health-row"><b>Mode</b><span>${esc(integ.mode||'sandbox')} ${oauth?'(OAuth connected)':'(OAuth not connected)'}</span></div><div class="health-row"><b>Sender configuration</b><span>${integ.config?.pick_code?'Configured':'Check before booking'}</span></div><div class="health-row"><b>OAuth tokens</b><span>${oauth?'Encrypted server-side in Supabase Vault':'Not stored yet'} — never shown in browser</span></div><div class="health-row"><b>Payment safety</b><span>Live postage actions remain gated; staging connection does not authorize production shipping</span></div></div><div class="row-actions" style="margin-top:14px"><button class="btn ${oauth?'':'primary'}" onclick="epConnect()">${oauth?'Reconnect EasyParcel':'Connect EasyParcel'}</button></div></section><section class="card" style="margin-top:16px"><div class="card-head"><h2>Order Shipping</h2></div><div class="table-wrap"><table class="table"><thead><tr><th>Order</th><th>Payment</th><th>Shipment</th><th>EasyParcel</th><th>Tracking / Waybill</th><th>Actions</th></tr></thead><tbody>${orders.length?orders.map(o=>{const s=map.get(o.id),ep=s?.metadata?.easyparcel_order_no||'';return `<tr><td><b>${esc(o.order_number)}</b><br><span class="muted">${dt(o.created_at)} · ${money(o.grand_total)}</span></td><td>${esc(o.payment_status)}</td><td>${esc(s?.status||'not_booked')}<br><span class="muted">${esc(s?.service_name||'')}</span></td><td>${ep?`<b>${esc(ep)}</b><br><span class="muted">${esc(s?.metadata?.booking_status||'')}</span>`:'—'}</td><td>${s?.tracking_number?`<b>${esc(s.tracking_number)}</b><br>`:''}${s?.tracking_url?`<a href="${esc(s.tracking_url)}" target="_blank" rel="noopener">Track ↗</a> `:''}${s?.waybill_url?`<a href="${esc(s.waybill_url)}" target="_blank" rel="noopener">Waybill PDF ↗</a>`:''}</td><td><div class="row-actions"><button class="btn" ${epReady?'':'disabled'} onclick="epRate('${o.id}')">Check Rates</button>${ep?`<button class="btn" ${epReady?'':'disabled'} onclick="epSync('${o.id}')">Sync Status</button>`:''}</div></td></tr>`}).join(''):'<tr><td colspan="6"><div class="empty">No staging orders yet.</div></td></tr>'}</tbody></table></div></section>`}
window.epConnect=async()=>{try{show('Connect EasyParcel','Starting secure OAuth authorization…','<div class="notice">Preparing EasyParcel authorization. No Postman is required.</div>','');const t=await token();const r=await fetch('/api/easyparcel/connect',{headers:{authorization:`Bearer ${t}`}});const j=await r.json();if(!r.ok||!j.authorization_url)throw new Error(j?.error||'EasyParcel authorization could not be started.');closeOpsDialog();window.location.href=j.authorization_url}catch(e){show('EasyParcel Connection','No credentials were exposed.',`<div class="notice error">${esc(e.message||String(e))}</div>`)}};
window.epRate=async orderId=>{try{show('EasyParcel Rate Check','Calling the staging shipping adapter…','<div class="notice">Checking rates…</div>','');const j=await api('rates',{order_id:orderId});const qs=j.quotes||[];show('EasyParcel Rates',`${qs.length} rate(s)`,qs.map(q=>`<div class="order"><div class="order-head"><div><b>${esc(q.service_name||q.courier_name||q.service_id)}</b><div class="muted">${esc(q.courier_name||'')} · ${esc(q.service_id)}</div></div><b>${money(q.rate)}</b></div><div class="row-actions"><button class="btn primary" onclick="epBook('${orderId}','${q.id}')">Create Staging Booking</button></div></div>`).join('')||'<div class="empty">No rates returned.</div>')}catch(e){show('EasyParcel Rate Check Failed','No booking was created.',`<div class="notice error">${esc(e.message||String(e))}</div>`)}};
window.epBook=async(orderId,quoteId)=>{try{show('Create EasyParcel Booking','Staging only. Live postage remains protected.','<div class="notice">Creating booking…</div>','');const j=await api('book',{order_id:orderId,quote_id:quoteId});show('EasyParcel Booking Created','No live success is assumed.',`<div class="notice"><b>${esc(j.easyparcel_order_no||'Booking created')}</b><br>${esc(j.courier||'')} ${j.price!=null?'· '+money(j.price):''}</div>`,`<button class="btn primary" onclick="closeOpsDialog();openOpsView('shipping')">Done</button>`)}catch(e){show('EasyParcel Booking Failed','No EasyParcel payment was made.',`<div class="notice error">${esc(e.message||String(e))}</div>`)}};
window.epSync=async orderId=>{try{show('Syncing EasyParcel','Checking current parcel status…','<div class="notice">Syncing…</div>','');const j=await api('status',{order_id:orderId});show('EasyParcel Status Updated',esc(j.easyparcel_status||'Status updated'),`<div class="notice">Shipment record synchronized.</div>`,`<button class="btn primary" onclick="closeOpsDialog();openOpsView('shipping')">Done</button>`)}catch(e){show('EasyParcel Sync Failed','Existing shipment data was kept.',`<div class="notice error">${esc(e.message||String(e))}</div>`)}};
const prev=window.openOpsView;window.openOpsView=view=>view==='shipping'?render():prev(view);
})();
